# ⚙️ Sección 10: Docker - Arguments y Environment Variables

---

## 🌐 [Introducción: El Ciclo de Vida de las Variables](https://vsupalov.com/docker-arg-env-variable-guide/)

En `Docker`, la gestión de configuraciones se divide en dos mundos: el tiempo de construcción `(Build-time)`
y el tiempo de ejecución `(Run-time)`. Comprender la diferencia entre `ARG` y `ENV` es vital para crear imágenes
portables y seguras.

### 🗺️ Mapa de Disponibilidad

No todas las variables viven para siempre. El siguiente esquema resume cuándo nace
y dónde muere cada tipo de variable  `ARG` y `ENV`:

![01.png](assets/section-10/01.png)

### 🏗️ ARG: Build-time Arguments

Las variables definidas mediante `ARG` son **efímeras**. Su propósito es inyectar valores durante el proceso de
empaquetado de la imagen.

- `Alcance`: Solo existen dentro del `Dockerfile`.
- `Limitación`: Los procesos internos del contenedor (como tu aplicación Spring Boot o scripts en CMD) no tienen acceso
  a estos valores.
- `Interactividad`: Si defines un `ARG` sin valor por defecto, Docker exigirá que se proporcione mediante la bandera
  `--build-arg` durante el comando `docker build`.

````bash
# Si no se pasa el valor en el build, Docker lanzará un error o advertencia
ARG APP_VERSION
````

### 🚀 ENV (Build-time and run-time Environment Variables)

Las variables `ENV` están disponibles tanto durante la construcción como para el futuro contenedor en ejecución.
**En el `Dockerfile` son utilizables tan pronto como se introducen con una instrucción `ENV`.**

A diferencia de `ARG`, los valores `ENV` persisten en la capa de metadatos de la imagen final y son plenamente
accesibles por el runtime del contenedor. Además, Docker permite la `inyección dinámica`, lo que significa que
cualquier valor `ENV` definido en el `Dockerfile` puede ser sobrescrito en tiempo de ejecución mediante las
banderas `-e` o `--env`.

### 📊 Comparativa Técnica: ARG vs. ENV

> Docker soporta dos tipos de variables que podemos configurar:
> - `Arguments` en tiempo de construcción de la imagen
> - `Environment` en tiempo e ejecución.

| Característica                     | **ARG** (Arguments)                      | **ENV** (Environment)                     |
|------------------------------------|------------------------------------------|-------------------------------------------|
| **Uso Principal**                  | Configurar la construcción de la imagen. | Configurar la ejecución de la aplicación. |
| **Visibilidad en Código**          | ❌ No accesible por la App.               | ✅ Accesible (ej. `System.getenv()`).      |
| **Instrucciones `CMD/ENTRYPOINT`** | ❌ No disponible.                         | ✅ Disponible.                             |
| **Persistencia**                   | ❌ Se pierde tras el `build`.             | ✅ Persiste en el contenedor.              |
| **Comando de Inyección**           | `docker build --build-arg ...`           | `docker run -e ...`                       |

> `Regla de Oro`:
> - Usa `ARG` para versiones de librerías, nombres de archivos temporales o configuraciones de compilación.
> - Usa `ENV` para credenciales de BD, URLs de servicios o parámetros que cambian entre Desarrollo y Producción.

## 🚀 Uso de Variables de Entorno `(ENV)` en `user-service`

En esta etapa, transformaremos el microservicio `user-service` en un componente dinámico. El objetivo es asignar
dinámicamente un **puerto de escucha** y el **perfil de ejecución** sin necesidad de reconstruir la imagen para cada
cambio.

## 🐳 Variables de Entorno `(ENV)` a nivel de Dockerfile

### 1. Preparación en Spring Boot (`application.yml`)

Lo primero que haremos será modificar el `application.yml` del `user-service` donde agregaremos la variable de
entorno `CONTAINER_PORT`.

````yml
server:
  port: ${CONTAINER_PORT:8001}
````

- `CONTAINER_PORT`: Nombre de la variable que buscaremos en el entorno de Docker.
- `8001`: Valor por defecto. Si la variable de entorno `CONTAINER_PORT` no fue definida, la app arrancará en el puerto
  `8001`.

### 2. Definición en el `Dockerfile`

En el `Dockerfile` del `user-service` utilizamos la instrucción `ENV` para establecer un valor predeterminado a nivel
de imagen. Esto asegura que la variable siempre exista.

````Dockerfile
# ... instrucciones anteriores ...

# Establecemos el puerto 8000 como valor por defecto de la imagen
ENV CONTAINER_PORT=8000

# Informamos que el contenedor escuchará en el puerto definido
EXPOSE ${CONTAINER_PORT}

ENTRYPOINT ["java", "org.springframework.boot.loader.launch.JarLauncher"]
````

- `ENV`, esta instrucción establece la variable de entorno `CONTAINER_PORT` con valor de puerto `8000`. Este valor
  estará en el entorno para todas las instrucciones posteriores en la etapa de construcción y puede ser reemplazado en
  línea.
- Al definir una variable de entorno con `ENV` `sí o sí debemos asignarle un valor`. La instrucción `ENV` requiere
  obligatoriamente la asignación de un valor inicial en el Dockerfile. A diferencia de `ARG`, no puede declararse
  'vacía'; esto asegura que la imagen tenga un estado predecible desde su construcción.
- Es importante recordar que, en esta situación, aunque `ENV` puede ser utilizado tanto en la construcción como en la
  ejecución del contenedor, cuando se usa en `EXPOSE`, su efecto es únicamente durante la fase de construcción de la
  imagen. Así que en este caso, el `ENV` está funcionando solo para la configuración del puerto en la imagen
  construida.

**💡Nota**
> Recordemos que en el `application.yml` del `user-service` hemos definido como valor por defecto el `8001` si es que
> la variable de entorno `CONTAINER_PORT` no viene definida.
>
> Ahora, como en el `Dockerfile` estamos estableciendo explícitamente el `ENV CONTAINER_PORT=8000`, significa que
> esa variable siempre estará definida y con valor `8000`, por lo que el puerto por defecto `(8001)` del
> `application.yml` jamás se usará.

### 3. Construcción y Despliegue

Construimos la imagen del microservicio `user-service` con la nueva configuración:

````bash
D:\programming\spring\01.udemy\02.andres_guzman\08.docker_kubernetes\docker-kubernetes-2026 (feature/section-10)
$ docker image build -t user-service .\business-domain\user-service
[+] Building 18.6s (24/24) FINISHED
...
````

### 4. Ejecución del Contenedor

Al ejecutar, debemos ser coherentes con el mapeo de puertos `(-p)`. Si nuestra imagen define internamente el
puerto `8000`, el mapeo debe apuntar a ese valor:

````bash
$ docker container run -d -p 8001:8000 --rm --name c-user-service --network docker-k8s-net user-service
c1dbac18aaf7d25894abd04bcdad816da26b2fd433e7e9dda2553f10c4a97b6a
````

- `-p 8001:8000`, el puerto externo sigue siendo `8001`, recordemos que usamos ese valor para poder acceder desde
  nuestra máquina local al contendor. Por otro lado, el cambio que hemos realizado fue en el puerto interno `8000`.
  Esto significa que al interior del contenedor nuestra aplicación de Spring Boot usará dicho puerto. Para que eso
  suceda, el valor del puerto interno definido en el parámetro `-p 8001:8000` debe ser coherente con el valor que le
  definimos a la variable `ENV CONTAINER_PORT=8000` dentro del `Dockerfile`.

Vemos los contenedores que tenemos creados.

````bash
$ docker container ls -a
CONTAINER ID   IMAGE                 COMMAND                  CREATED         STATUS         PORTS                                         NAMES
c1dbac18aaf7   user-service          "java org.springfram…"   2 minutes ago   Up 2 minutes   0.0.0.0:8001->8000/tcp, [::]:8001->8000/tcp   c-user-service
5f08ce05811b   postgres:17-alpine    "docker-entrypoint.s…"   24 hours ago    Up 2 hours     0.0.0.0:5433->5432/tcp, [::]:5433->5432/tcp   c-postgres
1ccddc608a1a   mysql:8.0.41-debian   "docker-entrypoint.s…"   24 hours ago    Up 2 hours     0.0.0.0:3307->3306/tcp, [::]:3307->3306/tcp   c-mysql
````

### 5. Verificación de Resultados

Al revisar los logs, confirmamos que Spring Boot ha reconocido la variable de entorno inyectada por Docker:

````bash
$ docker container logs c-user-service

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::                (v4.0.3)

2026-04-02T22:08:59.105Z  INFO 1 --- [user-service] [           main] d.m.user.app.UserServiceApplication      : Starting UserServiceApplication v0.0.1-SNAPSHOT using Java 25.0.2 with PID 1 (/app/BOOT-INF/classes started by root in /app) 2026-04-02T22:08:59.109Z DEBUG 1 --- [user-service] [           main] d.m.user.app.UserServiceApplication      : Running with Spring Boot v4.0.3, Spring v7.0.5
2026-04-02T22:08:59.110Z  INFO 1 --- [user-service] [           main] d.m.user.app.UserServiceApplication      : No active profile set, falling back to 1 default profile: "default"
2026-04-02T22:09:00.444Z  INFO 1 --- [user-service] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data JPA repositories in DEFAULT mode.
2026-04-02T22:09:00.546Z  INFO 1 --- [user-service] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 85 ms. Found 1 JPA repository interface.
2026-04-02T22:09:00.902Z  INFO 1 --- [user-service] [           main] o.s.cloud.context.scope.GenericScope     : BeanFactory id=e14e74eb-26bb-36e7-8fbc-96c726a7a059
2026-04-02T22:09:08.638Z  INFO 1 --- [user-service] [           main] o.s.boot.tomcat.TomcatWebServer          : Tomcat initialized with port 8000 (http)
2026-04-02T22:09:08.654Z  INFO 1 --- [user-service] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-04-02T22:09:08.655Z  INFO 1 --- [user-service] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/11.0.18]
2026-04-02T22:09:01.344Z  INFO 1 --- [user-service] [           main] b.w.c.s.WebApplicationContextInitializer : Root WebApplicationContext: initialization completed in 2166 ms
2026-04-02T22:09:01.785Z  INFO 1 --- [user-service] [           main] org.hibernate.orm.jpa                    : HHH008540: Processing PersistenceUnitInfo [name: default]
2026-04-02T22:09:01.925Z  INFO 1 --- [user-service] [           main] org.hibernate.orm.core                   : HHH000001: Hibernate ORM core version 7.2.4.Final
2026-04-02T22:09:02.612Z  INFO 1 --- [user-service] [           main] o.s.o.j.p.SpringPersistenceUnitInfo      : No LoadTimeWeaver setup: ignoring JPA class transformer
2026-04-02T22:09:02.659Z  INFO 1 --- [user-service] [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Starting...
2026-04-02T22:09:03.202Z  INFO 1 --- [user-service] [           main] com.zaxxer.hikari.pool.HikariPool        : HikariPool-1 - Added connection com.mysql.cj.jdbc.ConnectionImpl@42150951
2026-04-02T22:09:03.205Z  INFO 1 --- [user-service] [           main] com.zaxxer.hikari.HikariDataSource       : HikariPool-1 - Start completed.
2026-04-02T22:09:03.338Z  INFO 1 --- [user-service] [           main] org.hibernate.orm.connections.pooling    : HHH10001005: Database info:
        Database JDBC URL [jdbc:mysql://c-mysql:3306/db_user_service]
        Database driver: MySQL Connector/J
        Database dialect: MySQLDialect
        Database version: 8.0.41
        Default catalog/schema: db_user_service/undefined
        Autocommit mode: undefined/unknown
        Isolation level: REPEATABLE_READ [default REPEATABLE_READ]
        JDBC fetch size: none
        Pool: DataSourceConnectionProvider
        Minimum pool size: undefined/unknown
        Maximum pool size: undefined/unknown
2026-04-02T22:09:04.205Z  INFO 1 --- [user-service] [           main] org.hibernate.orm.core                   : HHH000489: No JTA platform available (set 'hibernate.transaction.jta.platform' to enable JTA platform integration)
2026-04-02T22:09:04.271Z  INFO 1 --- [user-service] [           main] j.LocalContainerEntityManagerFactoryBean : Initialized JPA EntityManagerFactory for persistence unit 'default'
2026-04-02T22:09:04.534Z  INFO 1 --- [user-service] [           main] o.s.d.j.r.query.QueryEnhancerFactories   : Hibernate is in classpath; If applicable, HQL parser will be used.
2026-04-02T22:09:05.017Z  WARN 1 --- [user-service] [           main] JpaBaseConfiguration$JpaWebConfiguration : spring.jpa.open-in-view is enabled by default. Therefore, database queries may be performed during view rendering. Explicitly configure spring.jpa.open-in-view to disable this warning
2026-04-02T22:09:05.745Z  INFO 1 --- [user-service] [           main] o.s.b.a.e.web.EndpointLinksResolver      : Exposing 1 endpoint beneath base path '/actuator'
2026-04-02T22:09:05.837Z  INFO 1 --- [user-service] [           main] o.s.boot.tomcat.TomcatWebServer          : Tomcat started on port 8000 (http) with context path '/'
2026-04-02T22:09:05.851Z  INFO 1 --- [user-service] [           main] d.m.user.app.UserServiceApplication      : Started UserServiceApplication in 0.108 seconds (process running for 8.925)
````

### 6. Prueba de Conectividad

Aunque internamente la App corre en el `8000`, nosotros seguimos accediendo a través del puerto del Host (`8001`):

````bash
$ curl -v http://localhost:8001/api/v1/users | jq
>
< HTTP/1.1 200
< Content-Type: application/json
< Transfer-Encoding: chunked
< Date: Thu, 02 Apr 2026 22:12:39 GMT
<
[
  {
    "id": 1,
    "name": "Lesly",
    "email": "lesly@gmail.com",
    "password": "123456"
  }
]
````

### 💡 Puntos Clave Aprendidos

- `Flexibilidad`: Podemos cambiar el puerto interno de la aplicación sin tocar una sola línea de código Java.
- `Coherencia`: El puerto derecho en `-p host:container` debe coincidir con el valor de `ENV` (o el valor por defecto
  de la App).
- `Visibilidad`: Las variables `ENV` son "horneadas" en la imagen pero permanecen visibles y editables.
